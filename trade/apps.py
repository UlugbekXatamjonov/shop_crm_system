from django.apps import AppConfig


class TradeConfig(AppConfig):
    """Savdo, chek va qaytarish ilovasi konfiguratsiyasi"""
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'trade'
    verbose_name = 'Savdo'
